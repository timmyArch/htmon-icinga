require 'spec_helper'

describe Htmon::Icinga do
  it 'has a version number' do
    expect(Htmon::Icinga::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
